# Chatbot with Memory

This image shows the UI of a chatbot with memory, where previous conversations are saved and can be restarted. Currently, chats are stored in active session memory.

![Chatbot Preview](../assets/chatbot_with_memory.png)